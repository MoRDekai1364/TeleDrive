<Window x:Class="TeleDrive.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TeleDrive.WPF.ViewModels"
        xmlns:pages="clr-namespace:TeleDrive.WPF.Views.Pages"
        Title="TeleDrive"
        Width="1000" Height="640"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:FilesPageViewModel}">
            <pages:FilesPage />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TransfersPageViewModel}">
            <pages:TransfersPage />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsPageViewModel}">
            <pages:SettingsPage />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource PanelBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="TeleDrive" FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Margin="20,0,0,24" />

                <Button Content="Files" HorizontalContentAlignment="Left" Padding="20,10"
                        Background="Transparent" BorderThickness="0"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Command="{Binding ShowFilesCommand}" />

                <Button Content="Transfers" HorizontalContentAlignment="Left" Padding="20,10"
                        Background="Transparent" BorderThickness="0"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Command="{Binding ShowTransfersCommand}" />

                <Button Content="Settings" HorizontalContentAlignment="Left" Padding="20,10"
                        Background="Transparent" BorderThickness="0"
                        Foreground="{DynamicResource TextPrimaryBrush}"
                        Command="{Binding ShowSettingsCommand}" />
            </StackPanel>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding CurrentPageViewModel}" />
    </Grid>
</Window>
