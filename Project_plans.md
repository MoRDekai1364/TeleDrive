# TeleDrive_plans

| status | priority | change | content |
|---|---|---|---|
| completed | high | new | Phase 1 — Solution skeleton: `TeleDrive.sln`, `TeleDrive.Core.csproj`, `TeleDrive.WPF.csproj`, all interfaces (`ITelegramService`, `IChunkingService`, `IIndexService`), all models (`VaultFile`, `FileChunk`, `TransferItem`, `AppSettings`, `ConnectionMode`), `HashHelper`, `SettingsHelper`, empty service stubs. |
| completed | high | new | Phase 2 — Core services implemented: `ChunkingService` (streamed split/reassemble + SHA-256 verify), `IndexService` (SQLite local cache + pinned-message JSON index), `TransferOrchestrator` (bounded `Channels` queue, worker pool, pause/resume/cancel, exponential backoff), `BotApiTelegramService`, `MtProtoTelegramService`. |
| next in line | critical | mod | `ITelegramService` was extended mid-Phase-2 with `EditMessageTextAsync`, `PinMessageAsync`, `GetPinnedMessageAsync` to support the pinned-index-message pattern. Not yet compiled/verified — no dotnet SDK available in the build environment. Verify signatures against actual restored NuGet packages (Telegram.Bot v21.2.0, WTelegramClient v4.1.1) once built locally. |
| completed | high | new | Phase 3 — Setup Wizard: `App.xaml`/`App.xaml.cs` (startup, settings load, service init), `WizardWindow`/`WizardViewModel` (step nav), all 6 pages (Welcome, ChooseMode, BotApiSetup w/ live `TestConnectionAsync`, MtProtoSetup w/ OTP/2FA bridge, Storage, Done) with ViewModels. Placeholder `Dark.xaml`/`CommonConverters` created early since `App.xaml` needed them (Phase 5 will replace with real polish). |
| next in line | critical | bug | Bot API cannot programmatically create Telegram channels — Telegram platform limitation, not a code bug. `StoragePage`'s "auto-create" option only works in MTProto mode. Bot API users must pre-create a channel and add the bot as admin. Wizard copy/flow needs reconsidering for Bot API path. |
| no status | medium | bug | `MtProtoSetupPageViewModel`'s OTP/2FA input bridge blocks synchronously (`.GetAwaiter().GetResult()`) inside WTelegramClient's configFunc callback, waiting on UI-submitted OTP/password. Expected to work since the callback fires off the UI thread, but unverified — test for deadlock risk once built. |
| completed | high | new | Phase 4 — Main UI: `MainWindow` shell (sidebar nav, `MainViewModel`), `FilesPage`/`FilesPageViewModel` (virtualized `ListView` bound to `IIndexService`, local-cache-first load then remote refresh, Upload/Download/Refresh), `TransfersPage`/`TransfersPageViewModel` (live cards bound to `TransferOrchestrator` via a shared `IProgress<TransferItem>`, Pause/Resume/Cancel), drag-and-drop upload from Explorer on `FilesPage`. `App` now branches at startup: opens `MainWindow` directly if `VaultChannelId` is already set, otherwise the wizard, which now opens `MainWindow` on completion instead of just closing. Added `ByteSizeConverter` / `TransferStatusToBrushConverter` and `TransferItem.IsInProgress`/`IsPaused`/`IsActive` convenience flags. Not yet compiled — no dotnet SDK in the build environment, same caveat as Phase 2/3. |
| next in line | critical | bug | Verify Phase 4 actually compiles once a dotnet SDK is available — first real build of the whole solution end to end (Phases 1-4 combined). Fix any XAML binding / namespace issues that only surface at compile/runtime. |
| completed | high | new | Phase 5 — Themes + Settings: `Light.xaml` (mirrors `Dark.xaml` keys), `ThemeManager` (Windows registry `AppsUseLightTheme` detection + runtime `ResourceDictionary` swap, applied at startup and on theme change), `SettingsPage`/`SettingsPageViewModel` (connection mode + vault channel display, theme selector, concurrent-transfer-limit slider, rebuild-cache button), settings persisted via existing `SettingsHelper` on every change. `MainWindow`/`MainViewModel` gained a Settings nav entry. Not yet compiled — same caveat as prior phases. |
| completed | medium | mod | `TransferOrchestrator` worker pool is now resizable at runtime (`SetWorkerCount`) — per-worker `CancellationTokenSource` list instead of a fixed `Task` list, cancelling only the read-loop of removed workers so in-flight transfers aren't interrupted. `SettingsPageViewModel`'s concurrent-transfer-limit slider now calls `SetWorkerCount` immediately instead of requiring a restart. |
| completed | low | new | Phase 6 — Resilience: `TransferOrchestrator.SetRetryPolicy` makes max attempts / backoff base configurable at runtime, exposed via new `SettingsPage` sliders (`MaxRetryAttempts`, `RetryBackoffBaseMilliseconds` on `AppSettings`). Queue state (`Queued`/`InProgress`/`Paused` transfers) is persisted to `queue-state.json` in the working directory on every status change; `TransferOrchestrator.RequeuePersistedTransfersAsync` reloads it and re-enqueues surviving uploads/downloads, called from `App.OnStartup` after `InitializeServices()`. Note: resume restarts each file from chunk 0, it does not resume mid-chunk. |
| completed | low | new | Phase 6 — Polish: `CrashLogger` (Core/Helpers) writes unhandled exceptions to `%AppData%\TeleDrive\logs\crash-*.log` and prunes old logs; wired into `App.xaml.cs` via `DispatcherUnhandledException`, `AppDomain.UnhandledException`, `TaskScheduler.UnobservedTaskException`. `TrayIconManager` (WPF, uses `System.Windows.Forms.NotifyIcon`, `UseWindowsForms` enabled in `TeleDrive.WPF.csproj`) minimizes to tray with a Show/Exit context menu. `MainWindow.xaml` gained `InputBindings` for Ctrl+U (upload), F5/Ctrl+R (refresh), Ctrl+1/2/3 (nav). `FilesPage`'s file `ListView` gained a right-click `ContextMenu` (Download, Copy Name). Not yet compiled — same caveat as prior phases. |
| no status | low | new | Phase 6 — Not done: shell icon extraction for file-type icons in `FilesPage`'s list. Deferred, no blocker, just not implemented yet. |
| no status | low | new | Phase 7 — Android port (MAUI), Phase 8 — iOS port (MAUI). Both deferred until `TeleDrive.Core` is fully stable and Windows app is feature-complete. |
| no status | critical | bug | Nothing built has been compiled or run yet. First priority once dotnet/Visual Studio is available: restore NuGet packages, build `TeleDrive.Core` standalone, fix any Telegram.Bot / WTelegramClient API signature mismatches before touching WPF. |
